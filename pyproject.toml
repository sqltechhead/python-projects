[project]
name = "python-projects"
version = "0.1.0"
requires-python = ">=3.13"

dependencies = [
    "mypy>=1.19.1",
    "pre-commit>=4.5.1",
    "qrcode[pil]",
    "ruff>=0.15.7",
]

[tool.ruff.lint]
extend-select = ["D"]

[tool.ruff.lint.pydocstyle]
convention = "numpy"
